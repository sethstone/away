BUG LIST for Away
-----------------
Email bug reports to cameron@unbeatenpath.net

* don't handle all config.h settings from ./configure

* configure.in could use some work

* mail thread is foobarred - not cycling through mailboxes; doesn't
    sleep; code has been broken since the persist code was added

